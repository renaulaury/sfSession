{% extends 'base.html.twig' %}

{% block title %}Hello ModuleController!{% endblock %}

{% block body %}

<section>
    <div class="titre">
        <h3 >Liste des modules</h3>    
        <a href="{{ path('app_addModule') }}"><i class="fa-solid fa-plus"></i></a>    
    </div>

    <div class="listModule"> 
        {% for module in modules %}
            <p>
                {{ module.libelleModule }}
                <a href="{{ path('app_editModule') }}"><i class="fa-solid fa-pencil"></i></a>
                <a href="{{ path('app_deleteModule') }}"><i class="fa-solid fa-trash"></i></a>
            </p>
        {% endfor %}
        
    </div>
</section>



{% endblock %}
